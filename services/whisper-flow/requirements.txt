# whisper-flow STT service deps. ffmpeg is an OS package (see Dockerfile), not pip.
fastapi==0.115.*
uvicorn[standard]==0.32.*
faster-whisper==1.0.*
# faster-whisper imports `requests` directly, but huggingface-hub 1.x no longer pulls it
# in transitively, so pin it explicitly.
requests==2.32.*
python-multipart==0.0.*
numpy==1.26.*
