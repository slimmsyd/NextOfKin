// Stub for the `server-only` package under vitest. The real package throws if
// imported outside a React Server Component; in unit tests we import server
// modules directly, so this no-op stand-in lets them load.
export {};
