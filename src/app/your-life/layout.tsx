import type { ReactNode } from "react";

export const metadata = {
  title: "Your life · NextOfKin",
};

export default function YourLifeLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
